---

## 1. الفكرة الأساسية (بعد التبسيط)
- **المرشد** يملأ نموذجًا بسيطًا يتضمّن اسمه وبيانات الطالب والمواد التي حُرم منها.
- **الكنترول** يدخل إلى صفحة عامة تعرض كل المواد، ويختار مادة ليشاهد قائمة الطلاب المحرومين، ويستطيع طباعة تقرير PDF.
- لا حاجة لتسجيل دخول أو مصادقة.
- التصميم **Minimal** خفيف وسريع.

---

## 2. تدفق الاستخدام
### واجهة المرشد:
1. يفتح الموقع (صفحة واحدة).
2. يُدخل:
   - **اسمه** (نص حر).
   - **اسم الطالب**، **الرقم الجامعي**، **التخصص**.
   - يختار **المواد المحروم فيها** من قائمة منسدلة (يمكن اختيار أكثر من مادة).
   - إن لم يجد المادة، يضغط **"+ إضافة مادة جديدة"** ويُدخل اسمها.
3. يضغط **"حفظ"**، فتُسجل البيانات في قاعدة Supabase.
4. تظهر رسالة تأكيد، ويُمسح النموذج.

### واجهة الكنترول:
1. يفتح صفحة مخصصة (يمكن مشاركة رابطها فقط مع الكنترول).
2. يظهر جدول بجميع المواد.
3. يضغط على اسم المادة ليظهر تقرير:
   - اسم المادة.
   - قائمة الطلاب المحرومين (الاسم، الرقم الجامعي، التخصص، اسم المرشد الذي أضافه، تاريخ الإضافة).
4. زر **"طباعة PDF"** يحمّل التقرير كملف PDF منسق.
5. يمكن البحث داخل التقرير (اختياري).

---

## 3. التقنيات
- **Frontend:** React.js (Vite) + Tailwind CSS.
- **Backend/قاعدة البيانات:** Supabase (خطة مجانية) مع API مباشر.
- **تصدير PDF:** `jsPDF` + `jspdf-autotable`.
- **الاستضافة:** Vercel أو Netlify للـ Frontend، Supabase للبيانات.

---

## 4. هيكل قاعدة البيانات (بدون صلاحيات ولا أدوار)
### جدول `subjects` (المواد)
| العمود       | النوع         | وصف                        |
|--------------|---------------|----------------------------|
| id           | uuid (PK)     | معرّف فريد                 |
| name         | text (unique) | اسم المادة (لا يتكرر)      |
| created_at   | timestamptz   | تلقائي                     |

### جدول `deprivations` (الحرمان)
| العمود       | النوع         | وصف                                  |
|--------------|---------------|--------------------------------------|
| id           | uuid (PK)     | معرّف الحالة                         |
| student_name | text          | اسم الطالب                           |
| student_id   | text          | الرقم الجامعي                        |
| specialty    | text          | التخصص                               |
| subject_id   | uuid (FK → subjects.id) | المادة المحروم منها       |
| advisor_name | text          | اسم المرشد الذي أضاف الطالب          |
| created_at   | timestamptz   | تاريخ الإضافة (تلقائي)               |

- **قيد منع تكرار** (اختياري): يمكن إنشاء `UNIQUE` على (`student_id`, `subject_id`) إن أردنا منع إضافة نفس الطالب لنفس المادة مرتين.

---

## 5. الأمان والصلاحيات (مبسط)
- جميع البيانات **عامة** (لا RLS)، لكن رابط واجهة الكنترول يمكن أن يكون مخفياً (مثلاً مسار `/control`) ولا يظهر في الواجهة الرئيسية. يمكن إضافة مفتاح بسيط (كلمة سر) في الرابط للدخول لو أردت، لكن غير ضروري حسب الطلب.
- الواجهة الخلفية Supabase ستُعدّل إعداداتها لتسمح بالوصول العام للقراءة والكتابة من التطبيق (باستخدام `anon` key الآمن في الواجهة الأمامية مع تفعيل سياسة `Enable read/write access to all users` في إعدادات الجدول) – هذا يكفي لغرض النظام.

---

## 6. خطة التنفيذ خطوة بخطوة

### المرحلة 1: إعداد المشروع وقاعدة البيانات
1. إنشاء مشروع React باستخدام Vite + Tailwind.
2. ربطه بـ Supabase (مشروع جديد في supabase.com).
3. تشغيل أوامر SQL التالية في محرر Supabase لإنشاء الجداول:
   ```sql
   create table subjects (
     id uuid default gen_random_uuid() primary key,
     name text unique not null,
     created_at timestamptz default now()
   );

   create table deprivations (
     id uuid default gen_random_uuid() primary key,
     student_name text not null,
     student_id text not null,
     specialty text not null,
     subject_id uuid references subjects(id) on delete cascade,
     advisor_name text not null,
     created_at timestamptz default now()
   );
تعطيل Row Level Security (RLS) مؤقتاً لكلا الجدولين، أو إنشاء سياسة تسمح بكل العمليات للجميع:
create policy "all_access" on subjects for all using (true) with check (true);
وتكرارها لجدول deprivations.

إدراج بعض المواد الافتراضية (إن وجدت ملف المواد الأساسي) في جدول subjects عبر واجهة Supabase أو استيراد.

المرحلة 2: بناء واجهة المرشد (صفحة رئيسية)
صفحة واحدة / تحتوي على:

حقل إدخال "اسم المرشد".

حقل "اسم الطالب".

حقل "الرقم الجامعي".

حقل "التخصص".

مكون اختيار المواد (Multi-select) يجلب المواد من جدول subjects ويعرضها بقائمة يمكن البحث فيها والاختيار المتعدد.

زر صغير "+ مادة جديدة" يفتح نافذة منبثقة (Modal) لكتابة اسم المادة وإضافتها مباشرةً، ثم تنعكس في قائمة المواد.

زر "حفظ" يقوم بـ:

لكل مادة مختارة، إدراج صف في deprivations بالقيم المدخلة + advisor_name.

استخدام supabase.from('deprivations').insert([...]) دفعة واحدة.

في حال فشل بعض الإدراجات (مثلاً تكرار)، إعلام المستخدم.

بعد الحفظ الناجح، إظهار رسالة نجاح وإعادة تعيين الحقول (باستثناء اسم المرشد، ربما يبقى لتسهيل تكرار الإضافة).

المرحلة 3: بناء واجهة الكنترول
صفحة /control (أو أي مسار) تحتوي على:

جلب قائمة المواد كأزرار أو قائمة.

عند اختيار مادة، عرض جدول يضم:

اسم الطالب، الرقم الجامعي، التخصص، اسم المرشد، تاريخ الإضافة.

مصدر البيانات: استعلام من deprivations مع ربط subjects للحصول على اسم المادة.

زر "تصدير PDF":

يستخدم jsPDF و jspdf-autotable لإنشاء ملف PDF بعنوان (مثلاً: تقرير الحرمان – مادة: MATH101) وتاريخ التصدير.

الجدول يحتوي جميع الأعمدة.

ميزة بحث اختيارية: حقل نصي لتصفية الجدول حسب الاسم أو الرقم الجامعي.

زر "إلغاء حرمان" (اختياري): عند الحاجة لسحب حرمان طالب، يمكن تنفيذه عبر تحديث صف أو حذفه مع تأكيد.

المرحلة 4: التصميم والـ UI/UX
استخدم Tailwind لتحقيق مظهر بسيط ونظيف:

خلفية فاتحة، أزرار مدورة، هوامش واسعة.

النموذج في واجهة المرشد: ترتيب عمودي، حقول بحواف ناعمة.

واجهة الكنترول: قائمة المواد في شريط جانبي (اختياري على الشاشات الكبيرة) أو قائمة منسدلة في الأعلى، مع جدول أنيق.

تأكد من تجاوب الموقع مع الجوال (responsive).

المرحلة 5: الإطلاق والتحسينات الإضافية
رفع المشروع على Vercel أو Netlify.

تدريب المستخدمين الفعليين وتزويدهم برابط الموقع (للمرشدين) ورابط /control للكنترول.

تحسينات لاحقة (اختيارية):

رفع ملف Excel لقائمة حرمان دفعة واحدة.

تصدير CSV إضافة إلى PDF.

إمكانية إرسال تنبيه بريدي للكنترول عند إضافة حرمان جديد (باستخدام Supabase Database Webhooks).

إضافة حقل "ملاحظات" للحرمان.

سجل نشاط بسيط (جدول يسجل كل عملية إضافة/حذف مع الوقت).

7. هيكل المشروع (Frontend)
text
src/
├── components/
│   ├── AdvisorForm.jsx        # نموذج المرشد
│   ├── SubjectSelector.jsx    # قائمة اختيار المواد
│   ├── NewSubjectModal.jsx    # إضافة مادة جديدة
│   ├── ControlSubjectList.jsx # قائمة المواد للكنترول
│   ├── DeprivationReport.jsx  # تقرير المادة + جدول + زر PDF
│   └── Layout.jsx             # تنسيق عام
├── pages/
│   ├── AdvisorPage.jsx
│   └── ControlPage.jsx
├── lib/
│   └── supabaseClient.js      # إعداد Supabase
├── hooks/
│   └── useSubjects.js         # جلب المواد
└── App.jsx                    # توجيه الصفحات